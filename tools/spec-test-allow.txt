# spec-test-allow.txt - FUNCTION/PROPERTY vals not yet bound to a test
# artifact (ADR-018 D-18.1: SpecTest verifies=Seq(...) or .scn @verifies).
# Each name is a KNOWN interim gap that keeps check 6 green while locking
# the door: no NEW unbound FUNCTION/PROPERTY may appear without either a
# test binding or an explicit line here. One val name per line; remove a
# name when its test lands (the red test comes BEFORE the implementation,
# ADR-018 D-18.3). Maintained by hand per work package; never regenerate it
# wholesale with --update-test-allow (ADR-019 Work Order 07 section 7).

# WP-4 backend vertices: need L1 SpecTests against the RTL (arbitration timing, port structure, CSR map assembly), a harness debugReq input (funcDebugCommitBoundary), or an enabled bit-manipulation config (BitAluUnit is absent in v0)
funcCsrMapContribution
funcDebugCommitBoundary
funcIntegrateExternalBitAlu
propTrapSingleWriter

# WP-5 LSQ: needs an uncacheable PMA region in the harness or an L1 LSQ SpecTest (funcHeadMemGrant is bound by commit.headMemGrant)
propUncachedPerformedOnce
funcUncacheableAtHead

# WP-5 committed StoreBuffer: directed backpressure test needs the harness bus-stall knob on the data link
propStoreBufferLiveness

# WP-1 recurrence budgets and tag uniqueness: need the ipcCriticalEdge helper (L0) or a composed backend to observe (funcRobOlder is bound by rob.order)
propBranchRecoveryBudget
propLoadUseBudget
propPredictNextPcBudget
propRobTagUniqueness
propWakeupSelectBudget

# WP-0 doctrine / ADR-015 machine checks: enforced by tools/spec-check.py and the L3 ISA-model comparison, which need the real spec plugin and a retiring core
propGenerationTagScope
propGraphConsistency
propIsaRetireEquivalence
propLocalSpecCoverage
propPropertyToAssertion
propRawTopWiringOnly
propRegQueueTagged

# pre-ADR-019 gap carried unchanged (implemented or protected IP, owner-held tests)
funcBootDelay
funcClzOptimization
funcDecodeOperation
funcExecuteBitAlu
funcResetOnDisable
funcSingleBootPulse
propCounterBound
propKillBehavior
propSingleCycleValid

# WP-7 bus adapters: L1 TileLink monitor SpecTest
propDataBusCoherenceSeparate
propInstBusGetOnly

# WP-7 D-cache: needs an uncacheable PMA region in the harness or the L3 geometry sweep
funcDCacheUncached
propDCacheFunctionTransparent

# WP-6 SFENCE.VMA: not encodable by the protected assembler
funcDtlbFlush
funcItlbFlush
funcPtwFlush

# WP-7 I-cache: needs an uncacheable/executable PMA region in the harness, an L1 SpecTest, or the L3 geometry sweep
funcICacheUncachedFetch
propICacheFunctionTransparent
propICacheReadOnly
propICacheResponseOrder

# WP-6 SFENCE.VMA: the owner-protected assembler has no SFENCE.VMA mnemonic, so no .scn can issue it yet
propSfenceFlushesAll

# WP-2/3 predictor/FTQ internals: microarchitectural state is only observable by an L1 BranchPredictor/FTQ SpecTest once the RTL exposes it (ADR-019 obligation: history restored from the FTQ checkpoint)
funcBlockExitSelect
funcBtbLookup
funcHistoryRestore
funcPredictorTraining
funcRasPredict
funcSpeculativeHistoryUpdate
funcTageDirection
propHistoryRestoreExact
propOneTakenCfiPerBlock
propPredictFromPcOnly
propPredictorStateMicroarchitectural

# ADR-019C new names: bound by their L1 SpecTests as each vertex lands (removed then)
propBranchControlOnce
